"""PRD-43b — the Timing Engine (WHEN).

Measures the user's entry and exit timing at the days scale against their own
record. Nothing in this package produces market evidence: the strongest claim
available here is `tested_on_personal_record`, and §4.4 forbids importing
`deflated_sharpe` / `pbo` / `validated` vocabulary into it.
"""
