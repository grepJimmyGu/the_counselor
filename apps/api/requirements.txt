fastapi==0.115.12
uvicorn[standard]==0.34.2
sqlalchemy==2.0.40
psycopg[binary]==3.2.7
pydantic-settings==2.9.1
python-dotenv==1.1.0
httpx==0.28.1
pandas==2.2.3
numpy==2.0.2
python-dateutil==2.9.0.post0
pytest==8.3.5
pytest-asyncio==0.24.0
yfinance>=0.2.54
financedatabase>=2.1.1
anthropic>=0.97.0
beautifulsoup4>=4.13.0
bcrypt==4.2.1
python-jose[cryptography]==3.3.0
# SnapTrade read-only brokerage connection (slice 3). The official SDK is
# used rather than hand-rolled HTTP: every request is signed with a
# timestamp + HMAC scheme, and hand-rolling that is exactly the class of
# thing that fails subtly and silently (apps/api/CLAUDE.md trap #14).
snaptrade-python-sdk==13.0.3
# Direct, not transitive-via-python-jose: `Fernet` protects the per-user
# SnapTrade userSecret at rest, so this must not vanish if jose's extras change.
cryptography>=42.0.0
apscheduler==3.10.4
email-validator==2.2.0
google-auth>=2.29.0
stripe==11.4.1
# Stage 6a — analytics + transactional email. Both safe no-op until env vars set.
posthog>=3.0.0
resend>=2.0.0
# Share-card rendering. NOT previously a dependency — it was importable
# locally only via unrelated packages (matplotlib, plotly), which would have
# made the card work in dev and ImportError on Railway.
Pillow>=10.0,<12
